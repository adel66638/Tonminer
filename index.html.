<!DOCTYPE html>
<html lang="ar" dir="rtl">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>تعدين TON السحابي</title>
    <style>
        body { background-color: #0d1117; color: #58a6ff; font-family: Arial, sans-serif; text-align: center; padding-top: 50px; }
        .container { border: 1px solid #30363d; padding: 20px; border-radius: 10px; display: inline-block; background: #161b22; }
        #balance { font-size: 2.5em; color: #3fb950; margin: 10px 0; }
        button { background-color: #238636; color: white; border: none; padding: 10px 20px; border-radius: 5px; cursor: pointer; font-size: 1.1em; }
    </style>
</head>
<body>
    <div class="container">
        <h2>رصيد التعدين الحالي ⚡</h2>
        <div id="balance">0.000000</div>
        <p>عملة TON</p>
        <button onclick="alert('سيتم فتح محفظة Tonkeeper قريباً...')">ربط المحفظة للسحب</button>
    </div>
    <script>
        let b = 0;
        setInterval(() => { b += 0.000001; document.getElementById('balance').innerText = b.toFixed(6); }, 1000);
    </script>
</body>
</html>
